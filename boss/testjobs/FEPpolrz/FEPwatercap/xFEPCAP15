#!/bin/csh
#
#   simple script to copy all necessary file from the
#   test job fepcap to execute an fep calculation.
#   only the the Z-matrix needs to be present in the current
#   directory.
#
#   Usage, e.g. for the Z-matrix zmat.z
#           xFEPCAP15 zmat
#
#
echo 'starting aqueous fep job - no cutoffs'
mkdir $argv[1].aq
cd $argv[1].aq
cp ../${argv[1]}.z fepzmat
cp $BOSSdir/testjobs/feppolrz/FEPwatercap/f* .
csh fepcmd >& log
#
rm  *a
rm  *b
rm  *c
rm  *eq
rm  *otd
rm  *pltd
rm  feppar*
#
grep DeltaG f*sum >>aqsum
cp aqsum delgsum
$BOSSdir/miscexec/sumdelg
rm delgsum
echo 'finished aqueous fep job'
cd ..
#
exit
