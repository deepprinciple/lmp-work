# Bench Specs

This directory is for durable benchmark definitions and baseline specifications.

Keep here:

- baseline strategy documents
- validation-set definitions
- reusable YAML configurations that define accepted workflows

Do not use this directory for logs, live-monitor files, or large run outputs.
Those belong under `bench_runs/`.
