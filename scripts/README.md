# Scripts

This directory is for operational scripts that are useful to run, but are not
part of the reusable workflow library layer.

Examples:

- benchmark launch helpers
- parameter scans
- standalone post-processing entrypoints
- config generators

Legacy script entrypoints may still exist at their historical paths for
backward compatibility while this directory becomes the preferred home.
